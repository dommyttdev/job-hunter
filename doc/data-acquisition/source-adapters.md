# 求人サイトアダプター

- 状態: Draft

## 取得対象

利用者が登録した検索結果URLと、検索結果から得た求人詳細URLを取得する。共通処理が受け取る検索結果は、次の値だけを持つ。

| 値 | 内容 |
| --- | --- |
| `source_key` | `atgp`などの求人サイト識別子 |
| `external_job_id` | サイト内求人ID |
| `detail_url` | 絶対URLへ正規化した求人詳細URL |

ATGPの例では、`https://www.atgp.jp/search/top/search_result_detail/a076000000012t7ife`から`a076000000012t7ife`を抽出する。

## 規約・制約

アダプターは次の契約を実装する。名称は概念を示し、実装言語決定時に型として定義する。

| 操作 | 入力 | 出力・責務 |
| --- | --- | --- |
| `supports_search_url` | URL | 許可されたscheme、host、pathかを厳密に判定する。 |
| `normalize_search_url` | URL | 同じ検索条件を表すURLを安定した形式にする。追跡用queryは除去する。 |
| `parse_search_page` | 最終URL、HTML | 求人参照と次ページURLを返す。次ページも許可host内に限定する。 |
| `parse_job_reference` | 詳細リンク | 求人IDを検証・抽出し、詳細URLを正規化する。 |
| `classify_detail_page` | status、最終URL、HTML | `AVAILABLE`、`REMOVED`、`RETRYABLE_ERROR`、`UNEXPECTED`のいずれかを返す。 |
| `canonicalize_detail_html` | HTML | ハッシュ比較対象となる安定HTMLまたはテキストを返す。 |

アダプターはHTTP通信、DB、要約、通知へ依存しない純粋な解析処理を基本とする。アダプター一覧は起動コードの静的なマップへ明示登録し、自動走査や文字列からの動的importは行わない。

## 取得方式・頻度

- スケジューラーは監視対象の`next_run_at`を基に検索取得作業を投入する。
- 検索結果は次ページがなくなるまで巡回する。ただし循環URL、最大ページ数、最大求人件数で停止できるようにする。
- 詳細ページは求人単位で取得し、複数の監視対象間で共有する。
- サイト単位の同時実行上限と最小アクセス間隔を全ワーカーで共有する。
- `robots.txt`と利用規約により取得不可、またはログイン必須の場合は監視登録を拒否または停止する。

## データ変換

比較用ハッシュは、生HTMLそのものではなくアダプターが正規化したページ内容のUTF-8バイト列からSHA-256で計算する。保存する`job_revision.html`は取得した生HTMLとし、再解析できるようにする。

正規化では、意味のある求人本文を残したまま次のような変動要素を除外する。

- CSRFトークン、nonce、アクセス解析識別子
- 現在時刻、閲覧数、ランダムID
- script、style、コメントなど求人内容と無関係な要素
- 属性順、連続空白、改行など意味を変えない表現差

何を除外するかはサイト固有fixtureテストで固定する。共通の汎用クリーナーだけで意味を推測しない。

## エラー・再試行

| 状況 | 扱い |
| --- | --- |
| タイムアウト、DNS、接続切断 | 指数バックオフとjitterで再試行 |
| HTTP 429 | `Retry-After`を優先し、サイト全体の取得を抑制 |
| HTTP 5xx | 上限付き再試行 |
| HTTP 401、403 | 削除扱いにせず、設定・アクセス制限の異常として停止候補 |
| HTTP 404 | 詳細ページでは`REMOVED`、検索ページでは監視設定異常 |
| HTTP 200の掲載終了ページ | アダプターが明示的なマーカーで`REMOVED`と判定 |
| DOM解析失敗、想定外リダイレクト | 削除扱いにせず`UNEXPECTED`として隔離・通知 |

HTTPリダイレクトごとに遷移先hostを検証し、内部ネットワークや未許可hostへのアクセスを拒否する。レスポンスサイズと展開後サイズに上限を設ける。

## 監視

- 取得成功率、status code、応答時間、レスポンスサイズ
- 検索ページ数、抽出求人件数、ID解析失敗件数
- 詳細分類ごとの件数とハッシュ変更率
- 429件数、サイト別キュー滞留時間、最終成功時刻
- アダプターの`UNEXPECTED`増加をHTML構造変更の兆候として警告

## 関連文書

- [システム設計](../architecture/system-design.md)
- [求人変更の判定](../change-detection/change-rules.md)
- [0001 求人サイト差異を明示的なアダプターで分離する](../architecture/adr/0001-explicit-source-adapters.md)

## 新しいサイトを追加する手順

1. 利用規約、robots.txt、認証要否、許容アクセス頻度を確認する。
2. URL許可規則とサイト単位の取得設定を追加する。
3. `JobSourceAdapter`の具象実装を追加する。
4. 検索1ページ、ページネーション、詳細、有効、掲載終了、構造変更のHTML fixtureを追加する。
5. 共通契約テストにアダプターを明示登録する。
6. 起動コードの静的アダプターマップへ追加する。
7. ステージングで低頻度監視し、抽出件数と誤更新率を確認して有効化する。
