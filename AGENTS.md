# エージェント向けプロジェクトルール

## ドキュメント

[ドキュメントフォーマット](./doc/development/documentation-formats/README.md)に従ってください。

## コーディング

[エージェント向けコーディング規約](./doc/development/coding-guidelines.md)に従ってください。

## マルチエージェント運用

マルチエージェントは並列化よりも、主エージェントのコンテキストを要件、判断、最終結果へ限定するために使用します。

- ループエンジニアリング、自律的な反復実装、受入条件を満たすまでのfix-and-verifyを依頼された場合は、[loop-engineeringスキル](./.agents/skills/loop-engineering/SKILL.md)を使用する。
- 単純な一段階の作業ではサブエージェントを使わない。
- 調査、実装、レビューを委任する場合は、タスクごとに新しいサブエージェントを使用し、無関係なタスクへ再利用しない。
- サブエージェントには会話履歴全体を渡さず、目的、成果物、変更可能範囲、変更禁止範囲、参照すべき正本、受入条件、停止条件、返却形式からなるタスク憲章だけを渡す。
- 調査には`context_scout`を使用し、生の探索結果ではなく短いコンテキストカプセルを返させる。
- 実装には`implementer`を使用する。共有作業ツリーでは同時に複数の実装エージェントを動かさず、書き手を1つに限定する。
- 通常レビューには`reviewer`を使用し、実装者の判断過程を渡さず、正本、受入条件、実際の差分から独立評価させる。
- 認証、所有権、アカウント削除、トランザクション、冪等性、競合制御、誤通知につながる変更には`critical_reviewer`を使用する。
- 主エージェントへは結論、根拠、変更パス、検証結果、未解決事項だけを返し、生のログ、長い引用、試行錯誤を持ち込まない。
- 主エージェントはサブエージェントの最終結果から必要な要約を回収した直後に、その完了済みスレッドを明示的にcloseする。後続タスクをspawnする前に、不要な`Done`スレッドが残っていないことを確認する。
- 読み取り中心の調査と検証だけを安全な範囲で並列化し、書き込み中心の作業は並列化しない。
- 指定モデルを利用できない実行環境では、`context_scout`を`gpt-5.6-terra`の`medium`、Sol担当を利用可能な最上位のコーディングモデルで代替し、置換したことを完了報告へ記載する。

## Git 運用

Git を操作する前に、[エージェント向け Git ブランチ運用ルール](./doc/development/git-workflow.md)を確認し、従ってください。

特に、次のルールは必須です。

- `main` へ直接コミットしない
- 作業ブランチには `fix/`、`feat/`、`chore/`、`docs/` など変更内容を表す種別を使用する
- ブランチ名に `codex/` などのエージェント固有の接頭辞を付けない
- コミットメッセージは Conventional Commits 形式とし、説明は日本語で記述する
- ユーザーの未コミット変更や、タスクと無関係な差分を変更・破棄・コミットしない
